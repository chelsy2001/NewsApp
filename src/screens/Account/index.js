//import liraries
import React from 'react';
import { Text, View } from 'react-native';

// create a component
const Account = () => {
    return (
        <View>
            <Text>Account</Text>
        </View>
    );
};

export default Account;
